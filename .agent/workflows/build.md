---
description: Build all binaries
---
Build all Go binaries for the backend services.

// turbo-all
1. Navigate to backend: `cd tw-backend`
2. Run `make build` to compile all services
3. Verify binaries exist: `ls -la game-server ai-gateway`
