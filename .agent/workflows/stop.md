---
description: Stop all Docker services
---
Gracefully stop all running services.

// turbo-all
1. Navigate to backend: `cd tw-backend`
2. Run `make down` to stop all containers
3. Verify with `docker ps` that no containers are running
