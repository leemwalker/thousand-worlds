---
description: Run golangci-lint static analysis
---
Run linting to check for code quality issues.

// turbo-all
1. Navigate to backend: `cd tw-backend`
2. Run `make lint`
3. Fix any errcheck, unparam, or other critical warnings
4. Run lint again to verify fixes
